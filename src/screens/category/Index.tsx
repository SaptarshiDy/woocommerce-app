import React from "react";
import { Text, View } from "react-native";

import BaseLayout from '../../layouts/Base';

export default () => {
    return (
        <View>
            <Text>
                Product Categories
            </Text>
        </View>
    );
}